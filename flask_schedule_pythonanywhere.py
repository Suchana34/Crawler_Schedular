#we created a web scraping pipeline to pull Top Posts from Reddit and store them in a MongoDB collection. At this stage, 
# we still have to manually execute our crawler via the command-line interface (CLI) each time we want to scrape Reddit.


